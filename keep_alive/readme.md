# README

## Project Structure

```
keepalive_django/
├── manage.py
├── keepalive_django/       ← main project directory
│   └── settings.py / urls.py / ...
├── hotsearch/              ← application for hot search
│   ├── views.py            ← search view
│   ├── urls.py             ← lunch URL routing
│   ├── templates/
│   │   └── index.html      ← HTML template for search
│   └── utils/
│       └── log_utils.py    ← LogUtils
```
